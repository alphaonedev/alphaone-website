# AlphaOne website

Stealth placeholder for **AlphaOne LLC**.

Static HTML only. Deployed via Cloudflare Pages on push to `main`.

## Local preview

```bash
python3 -m http.server 8080
# open http://localhost:8080
```
